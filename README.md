# Worker Service

A Python background worker for processing jobs from the API backend.

## Overview
This service will connect to a queue (like Redis) and process background tasks, such as sending emails or processing data asynchronously.

## Architecture
See architecture.png for a high-level diagram.
